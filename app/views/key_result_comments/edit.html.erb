<% provide(:title, 'Comment編集') %>

<div class="flex flex-col flex-1 justify-center mb-8 mt-8">
  <h1 class="text-4xl text-center">コメント編集</h1>
  <p class="text-center font-bold">Key Result : <%= @key_result.title %></p>
    <%= form_with(model: @key_result_comment, local: true) do |f| %>
      <%= render 'form', f: f %>
    <% end %>
</div>
