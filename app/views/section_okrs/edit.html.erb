<% provide(:title, 'Section OKR編集') %>

<div class="flex flex-col flex-1 justify-center mb-8 mt-8">
  <h1 class="text-4xl text-center">Section OKR編集</h1>
  <%= form_with(model: @section_okr, local: true, class: "") do |f| %>
    <%= render 'form', f: f %>
  <% end %>
</div>
