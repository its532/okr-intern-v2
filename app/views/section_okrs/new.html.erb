<% provide(:title, 'Section Okr登録') %>

<div class="">
  <h1 class="">Section OKR登録</h1>
    <%= form_with(model: @section_okr, local: true, class: "") do |f| %>
      <%= render 'form', f: f %>
    <% end %>
</div>
