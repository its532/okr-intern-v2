<% provide(:title, 'Section 編集') %>

<div class="form-container">
  <h1>Section名 編集</h1>
    <%= form_with(model: @section, local: true, class: "") do |f| %>
      <%= render 'form', f: f %>
    <% end %>
</div>
